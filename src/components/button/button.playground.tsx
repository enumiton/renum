import { Button } from './button';

const config: PlaygroundConfig = {
	title: 'button',
};

function Simple() {
	return (
		<div>
			<Button>Button</Button>
		</div>
	);
}

export { Simple };
export default config;
