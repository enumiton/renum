import { Loading } from './loading';
import './style/index.less';
const config = {
	title: 'loading',
};

function Example() {
	return (
		<Loading active />
	);
}

export { Example };
export default config;
