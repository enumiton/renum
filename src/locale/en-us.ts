import type { Locale } from './interface';

const locale: Locale = {
	locale: 'en-US',
	close: 'Close',
	dialog: {
		primaryButtonText: 'Ok',
		cancelButtonText: 'Cancel',
	},
};

export default locale;
